# M1 Validation - Renderer Mic IPC

Date: `2026-03-12`  
Milestone: `M1`  
Acceptance mapping: `PLAN.md` M1 items 5, 7, 8 and M1 testing requirements

## Objective

Validate the renderer-to-main microphone frame IPC path and verify the UI receives level updates while recording.

## Steps Executed

1. Build and run Playwright M1 smoke suite:
   - `npm run smoke:playwright`
2. Focused validation test executed as part of suite:
   - `tests/smoke/app-launch.spec.ts` test: `audio level UI reacts to mic frame events`
3. Confirmed flow under Electron runtime:
   - renderer calls `window.scribejam.sendMicFrames(...)`
   - main validates payload and ingests mic frames
   - main emits `audio:level`
   - renderer level indicator updates above `0%`

## Observed Evidence

- Test assertion passed for mic level increase:
  - `tests/smoke/app-launch.spec.ts:144`
- Preload bridge method used for IPC:
  - `src/preload/index.ts:22`
- IPC channel + payload contract:
  - `src/shared/ipc.ts:6`
  - `src/shared/ipc.ts:30`
  - `src/shared/ipc.ts:69`
- Main ingestion + validation:
  - `src/main/ipc-handlers.ts:98`
  - `src/main/audio/mic-capture.ts:8`
  - `src/main/audio/audio-manager.ts:61`
- Level event emission:
  - `src/main/audio/audio-manager.ts:100`
  - `src/main/ipc-handlers.ts:31`

## Raw Audio Persistence Audit

- Searched for audio file writes and raw audio artifact extensions in source:
  - no writes in audio modules
  - only settings/secrets JSON writes in settings modules
- Relevant files:
  - `src/main/audio/audio-manager.ts`
  - `src/main/audio/system-capture.ts`
  - `src/main/settings/settings-store.ts:94`
  - `src/main/settings/secure-secrets.ts:82`

## Result

`PASS` for renderer mic IPC and mic level event path in automated Electron smoke coverage.

## Notes / Residual Risk

- This validation demonstrates renderer-originated mic frame transport over typed IPC and downstream UI reaction.
- A physical microphone permission-granted run with live voice input is still recommended during M2 hardening for additional hardware-level confidence.
